---
interface Props {
  company: string
  date: string
  task: string[]
}

const { company, date, task } = Astro.props
---
  <div class="relative mb-8 ">
    <div
        class="absolute w-3 h-3 bg-cyan-500 rounded-full left-[-4.7%] top-1/2 transform -translate-y-2 ring-4 ring-cyan-500/20"
    >
    </div>
    <h3 class="text-xl font-bold text-white">
        {company}
    </h3>
    <p class="text-sm text-cyan-400 mb-2">
        {date}
    </p>
    <div class="text-gray-300 inline-block text-left">
        {task.map((t) => (
            <p>• {t}</p>
        ))}
    </div>
</div>