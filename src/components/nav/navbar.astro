<nav class="fixed w-full bg-transparent backdrop-blur-sm z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <!-- Logo -->
        <div class="flex-shrink-0">
          <span class="text-white font-bold text-xl">TuLogo</span>
        </div>
        
        <!-- Enlaces para desktop -->
        <div class="hidden md:block">
          <div class="ml-10 flex items-center space-x-4">
            <a 
              href="#experiencia" 
              class="nav-link relative text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.6)]"
            >
              Experiencia
              <span class="nav-link-underline"></span>
            </a>
            <a 
              href="#Proyects" 
              class="nav-link relative text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.6)]"
            >
              Proyectos
              <span class="nav-link-underline"></span>
            </a>
            <a 
              href="#education" 
              class="nav-link relative text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.6)]"
            >
              Educación
              <span class="nav-link-underline"></span>
            </a>
            <a 
              href="#About" 
              class="nav-link relative text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.6)]"
            >
              Sobre mí
              <span class="nav-link-underline"></span>
            </a>
            <a 
              href="#contact" 
              class="nav-link relative text-white px-3 py-2 rounded-md text-sm font-medium transition-all duration-300 hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.6)]"
            >
              Contactos
              <span class="nav-link-underline"></span>
            </a>
          </div>
        </div>
        
        <!-- Botón para mobile -->
        <div class="md:hidden">
          <button class="text-white hover:text-gray-300 focus:outline-none">
            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </div>
    </div>
  </nav>
  
  <style is:global>
    .nav-link {
      position: relative;
    }
    
    .nav-link-underline {
      position: absolute;
      bottom: 0;
      left: 50%;
      width: 0;
      height: 1px;
      background-color: white;
      transition: all 0.3s ease;
    }
    
    .nav-link:hover .nav-link-underline {
      width: 80%;
      left: 10%;
    }
  </style>